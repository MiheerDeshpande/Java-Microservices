spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/MiheerDeshpande/microservice-spring-boot-config
          clone-on-start: true
          
server:
  port: 8085 
  
